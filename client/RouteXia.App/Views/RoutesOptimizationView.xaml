<Page
    x:Class="RouteXia.App.Views.RoutesOptimizationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vm="clr-namespace:RouteXia.App.ViewModels"
    Background="{StaticResource BgBaseBrush}"
    Title="Routes Optimization">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="32,24,32,32" MaxWidth="850">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,24">
                <TextBlock
                    Text="Active Connections &amp; Routes"
                    FontFamily="/Resources/Fonts/#Rajdhani"
                    FontSize="26"
                    FontWeight="Bold"
                    Foreground="{StaticResource TextPrimaryBrush}" />
                <TextBlock
                    Text="Manage multi-path game server routes, protocol redundancy, and server hops"
                    FontSize="12"
                    Foreground="{StaticResource TextMutedBrush}"
                    Margin="0,2,0,0" />
            </StackPanel>

            <!-- Multi-path Mode Banner -->
            <Border
                Grid.Row="1"
                Background="{StaticResource BgCardBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="14"
                Padding="20,16"
                Margin="0,0,0,24">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Flash24" FontSize="18" Foreground="{StaticResource AccentBrush}" Margin="0,0,8,0" VerticalAlignment="Center" />
                            <TextBlock Text="Multipath Engine (Zero Packet-Loss)" FontFamily="/Resources/Fonts/#Rajdhani" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center" />
                        </StackPanel>
                        <TextBlock Text="Routes your gaming UDP traffic over parallel redundant nodes. The fastest received packet wins instantly." FontSize="11" Foreground="{StaticResource TextMutedBrush}" Margin="26,4,0,0" TextWrapping="Wrap" />
                    </StackPanel>
                    <Border Grid.Column="1" Background="{StaticResource BadgeBgBrush}" BorderBrush="{StaticResource StatusGoodBrush}" BorderThickness="1" CornerRadius="{StaticResource RadiusMd}" Padding="12,6" VerticalAlignment="Center">
                        <TextBlock Text="DUAL-PATH ACTIVE" FontFamily="/Resources/Fonts/#Rajdhani" FontSize="11" FontWeight="Bold" Foreground="{StaticResource StatusGoodBrush}" />
                    </Border>
                </Grid>
            </Border>

            <!-- Server Routes List -->
            <StackPanel Grid.Row="2">
                <TextBlock
                    Text="OPTIMIZED RELAY ENDPOINTS"
                    FontFamily="/Resources/Fonts/#Rajdhani"
                    FontSize="12"
                    FontWeight="SemiBold"
                    Foreground="{StaticResource TextMutedBrush}"
                    Margin="0,0,0,12" />

                <ItemsControl ItemsSource="{Binding ActiveRoutesList}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:RouteNodeInfo}">
                            <Border
                                Background="{StaticResource BgCardBrush}"
                                BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="{StaticResource RadiusLg}"
                                Padding="18,14"
                                Margin="0,0,0,10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <!-- Region Code Badge -->
                                    <Border Grid.Column="0" Background="{StaticResource NavActiveBgBrush}" BorderBrush="{StaticResource AccentBrush}" BorderThickness="1" CornerRadius="{StaticResource RadiusMd}" Width="44" Height="36" VerticalAlignment="Center" Margin="0,0,14,0">
                                        <TextBlock Text="{Binding RegionCode}" FontFamily="/Resources/Fonts/#Rajdhani" FontSize="12" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>

                                    <!-- Node Details -->
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding RegionName}" FontFamily="/Resources/Fonts/#Rajdhani" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" />
                                        <TextBlock Text="{Binding Host, StringFormat='Host: {0}'}" FontFamily="/Resources/Fonts/#JetBrains Mono" FontSize="11" Foreground="{StaticResource TextMutedBrush}" Margin="0,2,0,0" />
                                    </StackPanel>

                                    <!-- Status Pill -->
                                    <Border Grid.Column="2" Background="{StaticResource BadgeBgBrush}" CornerRadius="{StaticResource RadiusSm}" Padding="10,4" VerticalAlignment="Center" Margin="0,0,14,0">
                                        <TextBlock Text="{Binding Status}" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" />
                                    </Border>

                                    <!-- Latency Badge -->
                                    <Border Grid.Column="3" Background="{StaticResource BadgeBgBrush}" BorderBrush="{StaticResource StatusGoodBrush}" BorderThickness="1" CornerRadius="{StaticResource RadiusMd}" Padding="12,6" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding LatencyMs, StringFormat='{}{0}ms'}" FontFamily="/Resources/Fonts/#JetBrains Mono" FontSize="12" FontWeight="Bold" Foreground="{StaticResource StatusGoodBrush}" />
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
